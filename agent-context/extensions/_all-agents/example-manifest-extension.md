# Example manifest extension

This file demonstrates a phase extension registered via `manifest.md`. It loads
for every phase because it lives under `_all-agents/`.

Remove or replace this file in target projects once you add your own extensions.
